<!DOCTYPE html>
<html>
  <head>
    <title>MilkTracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path 'favicon.ico' %> 
    
  </head>

  <body>
    <% unless notice.nil? %>
      <p class="alert_msg"><%= notice %></p>
    <% end %>
    <% if current_page?(root_url) %>
      <a href="/stats">
    <% else %>
      <a href="/">
    <% end %>
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 -1 30 5">
      <circle cx="11" cy="2" r=".5" />
      <circle cx="20" cy="2" r=".5" />>
      <path d="M 0 0 L 6 0 C 9 4 13 4 15 0 C 18 4 22 4 24 0 L 30 0" stroke="#000" stroke-width=".75" fill="none"/>
    </svg></a>

    <div class="container">
    
      <%= yield %>
    </div>
  </body>
</html>
